"""
SteelMPF calibration and evaluation (optimize, generalized fit, plots).

Executable entry points live in this package as scripts (run from repo root).
See ``scripts/README.md`` for how postprocess -> calibrate data flow fits together.
"""
