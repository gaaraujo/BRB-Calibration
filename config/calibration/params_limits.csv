# Box limits for L-BFGS-B in optimize_brb_mse.py and optimize_generalized_brb_mse.py.
# Repo root — calibrate.calibration_paths.PARAM_LIMITS_CSV; override with --param-limits on those scripts.
# Columns: parameter, lower, upper. Names in PARAMS_TO_OPTIMIZE but omitted here are unbounded (-inf, inf).
# Finite L < U uses internal z in [0,1] reparam (scripts/calibrate/lbfgsb_reparam.py).
# SteelMPF: cR1/cR2 Filippou R-degradation (R -> R0(1-cR1) as plastic history grows; keep cR1 < 1 for R > 0).
# cR2 > 0 (SteelMPF uses cR2 + ksi). R0 > 0 (floor 1 avoids numerically stiff limits for small stiffness ratios).
parameter,lower,upper
b_p,0.0,0.10
b_n,0.0,0.10
R0,1.0,50.0
cR1,0.02,0.99
cR2,0.001,3.0
a1,0.0,0.2
a3,0.0,0.2
